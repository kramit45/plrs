/**
 * Pure domain layer for the Personalized Learning Recommendation System (PLRS).
 *
 * <p>Types in this package and its sub-packages must remain free of framework
 * dependencies: no Spring, no JPA, no Jackson, no Lombok. Only the Java
 * standard library is permitted so the domain model stays portable, testable
 * in isolation, and readable as vanilla Java during the MCSP-232 viva.
 */
package com.plrs.domain;
